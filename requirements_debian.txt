# Debian has old version of docker and docker-compose, which does not support
# some of required functionality like escaping $ in compose file during
# serialization; use a newer version.
docker==7.1.0

requests==2.32.5
requests-oauthlib==2.0.0
jsonschema==4.26.0
python-dateutil==2.9.0.post0
pyyaml==6.0.3
debugpy==1.8.19
fabric==3.2.2

# pylibfdt requires GCC and python3-dev.
pylibfdt==1.7.2.post1
